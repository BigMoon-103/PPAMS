Welcome to our Project webstie to run it:
1)download the provided project folder to your machine

2)Make sure Node.js is installed

3)In the project folder, run:

npm install
npm start

4)Open http://localhost:3000
